#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@n: is the input value
 * Return:  positive if the input is negative, positive if positive
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	}
